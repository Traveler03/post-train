# 源码快照说明

这里保存项目文档直接引用的 `milly` 分支实现，不再依赖 GitLab 链接。目录结构保持原仓库相对路径，便于文档内跳转和核对。

为避免把仍可使用的凭据提交到 GitHub，快照做了最小安全处理：

- `plan_checkpoint_eval.py` 不再内置个人邮箱，调用时必须显式传入 `--user-email`。

2026-09-16 按要求移除了 AutoTask 专属文档、流水线和评测项目；保留 Pro 共用的转换、训练与评测模块。共用代码中的历史契约名，以及 Pro 真实流量中的提醒工具名，不等于保留独立 AutoTask 项目。

除此之外，保留文件沿用上传快照内容。它是证据快照，不保证可在仓库外独立运行；运行仍依赖原项目环境、内部服务和未纳入的公共模块。本次新增的 Spike 核验脚本位于 `scripts/spike`，与历史源码分开。
